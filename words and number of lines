%{
#include <stdio.h>
int words = 0, lines = 0;
%}

%%
\n        { lines++; }
[ \t]+    { /* ignore spaces/tabs */ }
[^ \t\n]+ { words++; }
%%

int main()
{
    yylex();
    printf("Number of lines = %d\n", lines);
    printf("Number of words = %d\n", words);
    return 0;
}
